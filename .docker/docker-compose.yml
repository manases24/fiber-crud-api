version: '3.7'
services:
  postgres:
    image: postgres:16-alpine3.19
    container_name: task-db
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=USR_EM
      - POSTGRES_PASSWORD=usr_em
      - POSTGRES_DB=task
    volumes:
      - ./postgres:/var/lib/postgresql/data

  pgAdmin:
    image: dpage/pgadmin4:7.6
    container_name: pgdb
    environment:
      - PGADMIN_DEFAULT_PASSWORD=superman
      - PGADMIN_DEFAULT_EMAIL=superman@google.com
    ports:
      - "8080:80"
    depends_on:
      - postgres

volumes:
  postgres:
    external: false

